# 假设记录（ASSUMPTIONS）

## [2026-06-02/M0.14-R1] Codex App 指定会话跳转使用官方 threads deep link

- **背景**：用户要求“优先尝试 Codex App 跳转查看完整对话；如果没有可用跳转协议，就降级为打开终端执行 `codex resume <sessionId>`”。用户随后补充了当前 Codex manual 官方 “Deep links” 小节：打开本地线程的正式格式是 `codex://threads/<SESSION_UUID>`。
- **假设**：第一版使用官方 deep link `codex://threads/<sessionId>`；如果 Electron `shell.openExternal` 返回失败，就降级为 macOS Terminal 执行 `codex resume <sessionId>`。
- **理由**：`sessionId` 就是 Codex 本地线程 UUID，来源于 `codex exec --json` 输出的 `thread_id` 或 Codex App `/status` 中显示的 thread ID；官方文档明确 `codex://threads/<SESSION_UUID>` 打开 local thread。
- **影响范围**：该 deep link 只适用于当前机器上存在的 Codex local thread；如果线程记录不存在，仍需要依赖终端 resume 或重新创建会话。
- **状态**：已确认

## [2026-04-27/M0.13-R22] 状态工作台不迁移旧 `dueAt` 与旧块书签

- **背景**：本轮需求明确“块属性改为新的状态字段，不做旧数据兼容”，并要求去掉右键菜单里的“添加文本块到待处理”功能。
- **假设**：旧 `dueAt` 截止时间不再作为工作台依据；旧 `stickyBookmarks` 中带 `blockId` 的手动待处理书签不迁移为 `workStatus`。
- **理由**：用户已明确本轮不兼容旧待处理和旧 `dueAt` 数据，直接切换能避免保留双工作流导致入口混乱。
- **影响范围**：如果用户历史数据里已有截止时间块或手动待处理块书签，它们不会自动出现在新状态工作台；需要用户通过右键 `状态标记...` 重新设置状态。
- **状态**：已确认

## [2026-04-27/M0.13-R22] 等待回看时间只排序不提醒

- **背景**：等待中支持可选 `waitReviewAt`，但本轮需求没有要求接入系统提醒或弹窗。
- **假设**：`waitReviewAt` 只用于等待中 Tab 排序和正文/列表徽标显示，不创建 `reminders` 记录，也不触发通知弹窗。
- **理由**：这样符合“状态工作台”范围，避免把本轮改造扩大到提醒调度体系。
- **影响范围**：到达等待回看时间后，条目会在等待中 Tab 靠前，但不会自动弹出提醒。
- **状态**：待确认

## [2026-04-07/M0.13-R21-simplify] 时间能力收敛为“仅截止时间”

- **背景**：用户确认采用“极简版：只保留截止时间”，明确不再保留开始时间语义。
- **假设**：正文块时间字段只保留 `dueAt`；开始时间相关入口、状态文案和排序语义全部移除。
- **理由**：用户更关心“哪些内容最急、最该切过去”，而不是“晚点再开始做”；只保留截止时间可以显著降低理解成本和界面复杂度。
- **影响范围**：编辑器块属性、Sticky 右键菜单、自定义时间窗口、正文徽标文案、待处理筛选和排序规则。
- **状态**：已确认

## [2026-04-07/M0.13-R21-simplify] 不兼容旧 `startAt` 数据

- **背景**：在决定收敛为“仅截止时间”后，仍有两种实现路径：兼容旧 `startAt` 数据，或直接放弃兼容。
- **假设**：本轮不兼容历史 `startAt` 数据；旧数据不做迁移、不做自动映射到 `dueAt`。
- **理由**：这是用户明确决策，可以换来更干净的单时间模型和更少的维护分支。
- **影响范围**：如果已有历史块写入过 `startAt`，本轮起将不再被识别为时间条目；新写入路径也会主动移除残留 `startAt` 属性。
- **状态**：已确认

## [2026-04-07/M0.13-R21] 时间信息挂在正文块，不新增任务级时间字段

- **背景**：需求同时提到“文本块开始时间/截止时间”“今天/1 小时内筛选”“快速跳转正在处理或即将处理的事情”，而当前系统里已经同时存在待处理、优先级、提醒三套入口。
- **假设**：本轮把时间能力定义为“正文块级元数据”，直接写入 Tiptap 正文块属性；不新增 `tasks` 表任务级时间字段，也不复用 `reminders` 表。
- **理由**：用户要处理的是“某一段具体内容什么时候开始看、什么时候最晚完成”，不是整个任务统一只有一个时间；块级实现也更贴近当前 sticky 待处理的块定位与跳转模型。
- **影响范围**：编辑器 schema、正文块渲染、sticky 待处理排序与过滤；若后续要补“任务级截止时间”，需要额外设计块级时间与任务级时间的冲突规则。
- **状态**：待确认

## [2026-04-07/M0.13-R21] 保留手动待处理，并与时间块共用 Sticky 单一入口

- **背景**：当前 Sticky 已有“添加文本块到待处理”工作流；新需求又引入开始/截止时间，如果再单独造一套时间面板，会让入口继续分散。
- **假设**：保留现有手动待处理；带时间的块也进入 Sticky 同一个“待处理”入口。`全部` 视图同时显示两类条目，其中有时间条目按最近时间排序，无时间手动条目排在后面并保留现有手动顺序；`今天` 与 `1 小时内` 只显示有时间条目。
- **理由**：这样既不打断已有工作习惯，也能把“当前该处理什么”统一收束到 Sticky 的一个入口里，而不是再增加第四套时间入口。
- **影响范围**：待处理弹层的数据组织、底部左右切换顺序、筛选结果；若后续想完全废弃手动待处理，需要迁移现有 `stickyBookmarks` 中的块级书签数据。
- **状态**：待确认

## [2026-04-07/M0.13-R21] 右键快捷时间默认写入截止时间，自定义窗口再选择时间类型

- **背景**：用户要求右键可快捷设置 `5 分钟`、`10 分钟`、`30 分钟`、`1h`、`2h`，也要求弹出自定义时间窗口，但未明确这些快捷值默认对应开始时间还是截止时间。
- **假设**：右键快捷项默认全部设置“截止时间”；自定义时间窗口中再让用户选择“开始时间”或“截止时间”，并填写精确日期时间。
- **理由**：快捷时间最常见的语义是“多久内要做完”；把“开始时间”收进自定义窗口能控制右键菜单长度，避免菜单继续膨胀。
- **影响范围**：Sticky 右键菜单结构、自定义时间窗口字段设计、块时间互斥更新逻辑。
- **状态**：已推翻（2026-04-07：极简版仅保留截止时间，自定义窗口不再选择时间类型）

## [2026-04-07/M0.13-R21] 首轮只在 Sticky 落地块时间工作台

- **背景**：用户要求“只看今天”“只看 1 小时内”“快速跳转正在处理或即将处理的事情”，这些都更贴近 Sticky 当前的连续处理流，而 Library 已经同时承担任务树、搜索、详情页等多种入口。
- **假设**：第一版时间能力只在 Sticky 落地：右键设置时间、正文倒计时显示、待处理筛选与最近切换都只做 Sticky；Library 暂不新增 Today/1 小时内 Tab。
- **理由**：先把“执行工作台”收敛清楚，避免同时改 Library 与 Sticky 又制造新的入口分散。
- **影响范围**：实现范围、测试范围与用户文档；后续若要把时间视图扩到 Library，可复用同一块时间数据但需另行设计列表视图。
- **状态**：待确认

## [2026-04-07/M0.13-R21] 页面内容按 2 分钟刷新，倒计时按 1 分钟本地更新

- **背景**：用户要求“每 2 分钟和切换页面时刷新当前页面的内容，以及文本前的倒计时”，但若倒计时也严格 2 分钟一跳，分钟数会明显滞后。
- **假设**：Sticky 当前页面内容按“切换页面时立即刷新 + 每 2 分钟重新拉取一次”执行；倒计时文案在前端本地按 1 分钟粒度更新，并在页面刷新后重新校正。
- **理由**：这样既满足“跨窗口内容别太陈旧”的要求，也能让倒计时保持分钟级准确，不需要为每条倒计时都频繁访问数据库。
- **影响范围**：`App.tsx` / `StickyView.tsx` 的定时刷新策略、倒计时计算函数与性能边界。
- **状态**：待确认

## [2026-04-07/M0.13-R21] 首轮不为 taskLink 卡片挂时间

- **背景**：当前正文里既有普通文本块，也有 `taskLink` 子任务链接卡片；用户需求只明确了“给文本块加上开始时间和截止时间”。
- **假设**：第一版仅支持 `paragraph`、`heading`、`listItem`、`taskItem` 这四类正文块设置开始/截止时间，不支持对子任务链接卡片 `taskLink` 直接设时间。
- **理由**：`taskLink` 一旦有时间，会牵连父子任务、卡片样式和完成态同步，复杂度显著高于普通文本块；先把正文块路线做稳更安全。
- **影响范围**：编辑器属性挂载范围、右键菜单命中范围、倒计时渲染类型判断。
- **状态**：待确认

## [2026-04-07/M0.13-R21] 时间队列按 Sticky 共享根整棵活跃子树汇总

- **背景**：需求提出“只看今天”“1 小时内要做的事情”“快速跳转正在处理或即将处理的事情”，但未明确这些时间条目只看当前页面，还是跨页面汇总。
- **假设**：时间条目默认按当前 Sticky 共享根任务汇总，覆盖该根节点下所有未完成、未归档、未删除的子孙任务，而不是仅统计当前页。
- **理由**：如果只看当前页，`今天` 和 `1 小时内` 很难形成真正的工作台；按共享根汇总才能保持与 Sticky 跨页处理流一致。
- **影响范围**：新增主进程查询 `task:listTimedBlocksByRoot`、Sticky 待处理弹层计数与筛选范围。
- **状态**：待确认

## [2026-04-07/M0.13-R21] 同一块同时存在手动待处理和时间时合并为一条

- **背景**：需求要求“保留手动待处理”并新增块级开始/截止时间，但未明确当同一块两者同时存在时，列表应显示一条还是两条。
- **假设**：同一任务同一块如果既有手动待处理书签又有时间，只在待处理列表显示一条合并项；点击移除时会同时移除手动书签和块时间。
- **理由**：显示两条会造成重复噪声，也会让“左右连续切换”在同一块上重复停留；合并成一条更符合“这是同一个待处理对象”的心理模型。
- **影响范围**：Sticky 待处理列表的数据合并、移除行为、当前聚焦项顺序。
- **状态**：待确认

## [2026-03-16/M0.13-R18] 仅对存在可折叠正文范围的标题显示折叠按钮

- **背景**：用户确认采用“标题左侧小三角”作为入口，但未额外定义“空标题”或“标题后面直接接同级标题”的显示规则。
- **假设**：只有当某个标题后面确实存在可折叠内容时，才在该标题左侧显示折叠按钮；没有正文范围可收起的标题不显示按钮。
- **理由**：这能避免空标题左侧出现无效控件，也符合现有列表折叠“有子内容才显示入口”的交互惯例。
- **影响范围**：`Library` 与 `Sticky` 的标题折叠入口显示条件；若后续希望所有标题都显示按钮，需要调整标题范围判定与样式。
- **状态**：待确认

## [2026-03-16/Build-mac] 本地 mac 构建默认不做代码签名

- **背景**：`electron-builder` 在 mac 打包阶段自动发现本机 `Apple Development` 证书，但该证书不能用于当前 distribution 签名，导致 `codesign` 失败，无法生成本地安装包。
- **假设**：本轮将 `package.json > build > mac > identity` 设为 `null`，默认跳过 mac 代码签名，优先保证开发机可稳定产出 `.app` 与 `.dmg`。
- **理由**：当前用户目标是“能编译成 mac 应用直接启动”，不是“可对外分发的签名发布包”；跳过签名是最小改动且最稳妥的本地构建方案。
- **影响范围**：生成的 `dist/mac-arm64/Notes.app` 与 `dist/Notes-0.1.0-arm64.dmg` 为未签名本地产物；若后续要对外分发，需要补充 Apple Developer 签名证书、可能的 notarization（公证）配置，并恢复签名流程。
- **状态**：待确认

## [2026-03-05/M0.13-R17] 已打钩文本块显示开关默认仅作用于当前便签会话

- **背景**：需求提出“便签上右键增加显示/隐藏已经打钩的 checkbox 文本块”，未明确是否需要跨窗口/重启持久化。
- **假设**：本轮先实现为当前便签窗口内的本地 UI 状态，不写入数据库，不跨重启保留。
- **理由**：可最小改动快速交付核心交互，避免引入窗口状态 schema 变更与迁移风险。
- **影响范围**：关闭并重新打开便签后，显示状态会恢复为默认“显示已打钩项”。
- **状态**：待确认

## [2026-02-07/M0] 文档主版本以根目录为准

- **背景**：仓库中已有 `Docs/PRD.md`，但规则要求维护根目录核心文档体系。
- **假设**：后续以根目录 `PRD.md` 作为主版本，`Docs/PRD.md` 视为历史参考，不在本次改动中删除。
- **理由**：满足规则要求且避免一次性迁移导致外部引用断裂。
- **影响范围**：文档引用路径、团队协作入口。
- **状态**：待确认

## [2026-02-07/M0] 当前目标仅做文档对齐，不进行功能编码

- **背景**：用户需求为“根据 rules 和本地最新代码调整文档，不存在则创建”。
- **假设**：本轮仅输出文档治理产物，不改动业务逻辑代码。
- **理由**：严格遵循用户当前边界，避免范围蔓延。
- **影响范围**：`src/**` 不变，后续里程碑再做功能落地。
- **状态**：待确认

## [2026-02-07/M0] 里程碑从“文档校准”起算

- **背景**：规则要求里程碑驱动，且当前任务本质是文档治理。
- **假设**：将本次交付定义为 `M0`，后续功能补齐从 `M1` 开始。
- **理由**：便于追踪“文档基线 → 功能迭代”的演进。
- **影响范围**：`plan.md` 与 `DECISIONS.md` 的编号体系。
- **状态**：待确认

## [2026-02-07/M0.6] 会话收尾流程以参数化脚本实现

- **背景**：用户要求“提交并运行、总结改动、dev 启动截图、飞书通知”并可重复执行。
- **假设**：优先实现为独立 Python 脚本（`session_release.py`），通过参数控制是否跳过提交/dev/飞书步骤。
- **理由**：脚本可复用、易审计、与 skill 的低自由度执行目标一致。
- **影响范围**：`~/.codex/skills/session-release-bot/scripts/session_release.py` 与 `SKILL.md` 操作说明。
- **状态**：待确认

## [2026-02-07/M0.6] 截图优先使用系统全屏，其次 Playwright

- **背景**：需求强调“启动后对整个界面进行截图”，Electron 窗口截图可靠性依赖运行环境。
- **假设**：默认 `auto` 模式下在 macOS 优先 `screencapture` 全屏，非 macOS 或缺失命令时降级 Playwright 页面截图。
- **理由**：更贴近“整个界面”语义，同时保留跨平台兜底能力。
- **影响范围**：截图模式与产物 `ui-fullscreen.png` 的来源。
- **状态**：待确认

## [2026-02-07/M0.6] 飞书消息以文本摘要发送

- **背景**：用户仅要求“通过机器人发送改动”，未指定卡片样式与字段结构。
- **假设**：默认发送 `text` 消息，包含分支、提交状态、改动文件数、diff 统计摘要。
- **理由**：实现最稳健，避免卡片 JSON 兼容问题导致发送失败。
- **影响范围**：`send_feishu_message` 消息格式与可读性。
- **状态**：待确认

## [2026-02-07/M0.7] 会话收尾脚本默认不主动读取项目 `.env`

- **背景**：本次飞书联调首轮执行出现“缺少 webhook”，而仓库根目录已存在 `.env`。
- **假设**：`session_release.py` 继续保持“仅读取进程环境变量/命令行参数，不自动加载 `.env` 文件”的行为，测试时由外层命令显式 `source .env`。
- **理由**：保持脚本职责单一，避免引入对 dotenv 的隐式依赖与环境污染。
- **影响范围**：执行该 skill 时，调用方需显式注入 `FEISHU_BOT_WEBHOOK`（和可选 `FEISHU_BOT_SECRET`）。
- **状态**：已推翻

## [2026-02-07/M0.8] AI 摘要采用规则化离线生成

- **背景**：用户要求“AI总结当前改动内容描述（非具体代码）”并希望快速稳定执行。
- **假设**：先以脚本内规则化聚合（按文件类型/操作类型/diff 统计）生成“AI改动总结”，不引入外部 LLM 依赖。
- **理由**：零外部依赖、可离线执行、稳定可复现，同时满足“非代码细节叙述”的交付需求。
- **影响范围**：`ai-summary.md` 的文本风格偏结构化；后续若接入真实 LLM，需调整摘要生成模块。
- **状态**：待确认

## [2026-02-07/M0.9] 功能建议以单用户本地优先为默认前提

- **背景**：用户要求“基于当前实现补充可增加的功能”，但未指定是否优先协作/云同步场景。
- **假设**：本轮建议优先围绕“单用户本地桌面效率”排序，将协作、云同步、插件生态放在中长期。
- **理由**：当前代码基线是本地 SQLite + Electron 双窗口工作流，先补闭环与效率提升能更快产生可见价值。
- **影响范围**：建议文档中的优先级排序（P0/P1/P2）与阶段推进顺序。
- **状态**：待确认

## [2026-02-07/M0.10] P1 执行任务按四个里程碑拆分推进

- **背景**：用户要求基于 P1 建议生成执行任务，但未指定迭代批次数。
- **假设**：默认拆分为 P1-M1~P1-M4 四个里程碑，每个里程碑控制在 1~2 周并可独立验收。
- **理由**：四段拆分能平衡交付节奏与风险控制，便于逐步上线和回归验证。
- **影响范围**：`Docs/P1_EXECUTION_TASKS.md` 中路线图与后续计划排期方式。
- **状态**：待确认

## [2026-02-08/M0.11] 便签共享范围按“祖先链首个父任务”归类

- **背景**：用户要求“同一个父级任务下的便签共用状态”，但未指定多父关系下的归属优先级。
- **假设**：sticky 分组使用当前任务祖先链中的首个任务作为共享根（无祖先时使用自身）。
- **理由**：现有 `getAncestorChain` 已按创建顺序返回稳定路径，可在不改数据模型前提下落地共享语义。
- **影响范围**：`window:open` 打开策略、便签书签/皮肤共享范围、多父任务场景的分组归属。
- **状态**：待确认

## [2026-02-08/M0.12] 任务标题唯一性按“全库未删除任务”校验

- **背景**：用户仅明确“不支持出现同名子任务”，但 markdown 文本匹配同步若只做局部去重仍有歧义风险。
- **假设**：本轮将唯一性约束提升为“全库范围（`is_deleted = 0`）标题唯一”，并统一应用于新建与重命名。
- **理由**：能确保 `taskLink` 标题和 markdown checkbox 文本映射始终确定，避免跨父任务同名导致的同步错误。
- **影响范围**：`task:create` / `task:createFromBlock` / `task:update(title)` 及前端创建、重命名交互提示。
- **状态**：已推翻（2026-02-14 M0.13-R12：改为“同一父任务下兄弟子任务唯一”）

## [2026-02-08/M0.12] 关联 markdown checkbox 采用“标题完全匹配 + 独占匹配”

- **背景**：需求给出“链接块或文本匹配”作为关联前提，但未定义文本匹配的容错规则。
- **假设**：文本匹配采用 `trim + 大小写不敏感 + 完全相等`，且仅在标题唯一时触发同步。
- **理由**：实现简单、可解释、稳定，避免模糊匹配引入误关联。
- **影响范围**：`taskBlocksSync` 中 completion diff 解析与标题重命名回写规则。
- **状态**：待确认

## [2026-02-08/M0.13] 子任务选择流程先使用“序号/完整标题输入”

- **背景**：1.4 与 1.6 需要“选择已有子任务/目标父任务”，当前项目已有通用输入弹窗 `PromptModal`，尚无列表选择弹窗组件。
- **假设**：先复用 `PromptModal`，通过“输入序号或完整标题”的方式完成选择；后续再升级为可视化可搜索下拉/列表选择器。
- **理由**：在不引入新 UI 组件体系的前提下可快速完成闭环，降低实现复杂度并保持交互一致性。
- **影响范围**：`App.tsx` 中 `insertExistingChildLink` / `moveChildReference` 的选择交互体验。
- **状态**：已推翻（2026-02-09 M0.13-R1 改为顶部下拉选择）

## [2026-02-08/M0.13] 任务树拖拽目标先支持“作为子级”与“拖回根级”

- **背景**：1.5 提到拖拽改变层级，未明确是否必须支持同级前后排序与精确插入位置。
- **假设**：本轮先支持两种语义：拖到节点上 => 成为其子任务；拖到列表空白区 => 成为根任务。
- **理由**：当前 `edges` 模型无显式排序字段，先实现稳定的层级变更能力，再在后续迭代补同级排序。
- **影响范围**：`LibraryPanel` 的拖拽提示与 `edge:reparent` 行为；任务顺序仍由更新时间/创建关系决定。
- **状态**：待确认

## [2026-02-08/M0.13] 归档已完成子任务不删除父子边，仅做“子任务归档 + 父正文移除链接”

- **背景**：1.7 要求归档后正文移除，但未明确是否要删除 `edges` 关系。
- **假设**：执行归档时保留 `edges` 父子关系，仅将子任务 `isArchived=true` 并移除父正文 `taskLink` 引用。
- **理由**：保留结构关系可减少数据破坏风险，恢复或后续统计时可追溯；同时满足“正文瘦身”目标。
- **影响范围**：`task:archiveCompletedChildren` 的数据侧行为与后续“恢复链接”可操作性。
- **状态**：待确认

## [2026-02-09/M0.13-R3] 便签右键插入入口采用“扁平列表”而非二级子菜单

- **背景**：用户要求将 sticky 顶部“插入已有子任务”按钮改为右键菜单，并展示可插入子任务列表。
- **假设**：沿用当前 `ContextMenu` 组件能力，右键菜单直接展开为多条“插入子任务：xxx”扁平项，不额外引入二级子菜单结构。
- **理由**：可在最小改动范围内完成交互迁移，并保持现有菜单组件与事件模型稳定。
- **影响范围**：`StickyView` 非链接块右键菜单项数量随可插入子任务数量变化；若后续需要分组/二级菜单，需扩展 `ContextMenu` 数据结构。
- **状态**：待确认

## [2026-02-09/M0.13-R4] 任务树滚动仅处理纵向溢出

- **背景**：用户反馈任务树超长时显示不全，需增加滚动条以便浏览完整列表。
- **假设**：本轮仅修复纵向滚动（`overflow-y`），横向仍保持隐藏，长标题继续由现有 `truncate` 处理。
- **理由**：问题核心是纵向内容被裁切，最小改动即可解决主要可用性问题并避免引入横向滚动噪声。
- **影响范围**：`LibraryPanel` 任务树容器滚动行为与样式；若后续要支持横向查看完整标题需另行调整。
- **状态**：待确认

## [2026-02-09/M0.13-R5] 右键子任务列表采用“单菜单内折叠展开”

- **背景**：用户要求右键先点“插入子任务”再出现列表，同时要求长菜单不被便签截断。
- **假设**：在现有 `ContextMenu` 组件中扩展 `children`，采用同一菜单内折叠展开子项，不引入独立浮层级联子菜单。
- **理由**：实现复杂度更低，且可直接复用菜单容器的滚动能力，稳定解决“长菜单被截断”问题。
- **影响范围**：`ContextMenu` 的通用数据结构与渲染逻辑升级；其它菜单也可复用二级项能力。
- **状态**：待确认

## [2026-02-09/M0.13-R6] 独立右键菜单窗口先覆盖 sticky 场景

- **背景**：用户要求右下角右键时不被截断，并建议参考“皮肤设置”独立弹窗。
- **假设**：本轮先将独立弹窗菜单能力应用于 `sticky`，`library` 仍保留原页面内菜单渲染。
- **理由**：当前裁切问题集中在 sticky 便签窗口；先定向修复可控制改动范围并降低回归风险。
- **影响范围**：`sticky` 的菜单显示链路（show/hide/select IPC + popup window）；若后续需要统一，`library` 可复用同一能力。
- **状态**：待确认

## [2026-02-10/M0.13-R9] “待处理（n）”按钮按点击切换弹层显隐

- **背景**：用户反馈“点击便签上的待处理后没有任何反应”，需求重点是恢复可点击与可见反馈，未额外指定再次点击时的行为。
- **假设**：按钮点击行为采用“切换显隐”（首次点击展开、再次点击收起），同时保留点击空白处关闭。
- **理由**：切换行为符合常见下拉弹层交互习惯，且在最小改动下同时满足“可打开”和“可主动收起”。
- **影响范围**：`src/renderer/components/StickyView.tsx` 的待处理按钮点击逻辑；若后续要求“只允许点击空白关闭”，需调整为固定展开。
- **状态**：待确认

## [2026-02-10/M0.13-R10] 待处理条目跨页面定位采用“挂起定位 + 重试一次”

- **背景**：点击待处理条目时，目标块可能位于非当前任务页面，且编辑器内容切换存在异步渲染时序。
- **假设**：跨页面定位采用“先记录挂起目标，切换页面后在 `task/blocks` 更新时触发定位；若首次未命中再短延迟重试一次”。
- **理由**：在不引入复杂事件总线的前提下，能稳定覆盖页面切换与编辑器异步刷新场景。
- **影响范围**：`src/renderer/components/StickyView.tsx` 的待处理条目点击链路与 `scrollToBlock` 调用时机；若后续引入更强的编辑器 ready 事件，可替换当前重试策略。
- **状态**：待确认

## [2026-02-11/M0.13-R11] 待处理块锚点以右键点击位置为准

- **背景**：用户反馈同一文档不同位置文本块加入“待处理”时，仅有一条生效，表现为同页只能加一条。
- **假设**：在执行“添加文本块到待处理”时，优先使用右键菜单触发时记录的点击坐标对应文档位置作为块锚点，而不是沿用旧光标位置。
- **理由**：该行为与用户心理模型一致（右键哪个块就添加哪个块），且能在最小改动范围内修复“重复命中同块”的问题。
- **影响范围**：`src/renderer/components/StickyView.tsx` 中右键菜单定位链路与待处理块创建逻辑；若后续改为键盘触发入口，需要补充无鼠标坐标兜底策略。
- **状态**：待确认

## [2026-02-14/M0.13-R12] 根任务名称允许同名

- **背景**：本轮需求明确“兄弟级别的子任务不支持同名”，未明确是否也限制根任务同名。
- **假设**：仅对子任务关系做同父级重名校验，根任务（无父任务）允许同名。
- **理由**：与“子任务去重”语义一致，且避免把规则重新扩大回全局约束。
- **影响范围**：`task:create`、根任务重命名、任务树 quick add 的重名提示行为。
- **状态**：待确认

## [2026-02-14/用户文档] 使用帮助默认面向“已可打开应用”的终端用户

- **背景**：本次需求为“补充完善面向用户的使用帮助文档”，但未明确是否要覆盖安装/打包分发流程。
- **假设**：本轮《用户指南》默认面向“已安装并可启动 TodoNotes 的终端用户”，重点覆盖日常使用、导航与常见问题，不展开安装部署说明。
- **理由**：当前项目以功能迭代为主，用户已提供可用初稿且核心诉求集中在“如何拆分与导航记录任务”；保持文档聚焦可读性更高。
- **影响范围**：`Docs/用户指南.md` 的内容边界；若后续需要对外发布，可新增“安装与升级”章节或独立《安装手册》。
- **状态**：待确认

## [2026-02-24/M0.13-R13] 待处理锚点按“块ID + 行末偏移”记录

- **背景**：用户反馈 sticky 右键“添加文本块到待处理”后，后续光标定位记录不准确，应落到对应行末。
- **假设**：待处理条目在保留 `blockId` 的同时新增 `blockCursorOffset`（块内偏移），并按“最近文本行末”记录；跳转时优先使用该偏移恢复光标，缺省再退回块末尾。
- **理由**：仅依赖 `blockId` 无法稳定表达同块内的具体行位置；增加偏移可在最小改动下精确恢复定位，并兼容历史书签数据。
- **影响范围**：`src/renderer/components/StickyView.tsx` 的待处理新增/跳转/删除逻辑，`src/renderer/utils/blockScroll.ts` 的定位签名，`src/main/db/windowStateRepo.ts` 的书签反序列化字段解析。
- **状态**：待确认

## [2026-02-24/M0.13-R13-hotfix] 右键边界命中先归一化到最近文本位置

- **背景**：用户复测发现：在列表符号区或块边界右键添加“待处理”后，单击回跳光标仍可能偏到下一行中间。
- **假设**：记录锚点前先使用 `Selection.near` 将点击位置归一化到最近可编辑文本位置，再按文本块计算行末偏移。
- **理由**：`posAtCoords` 在边界区域可能返回父块/结构节点位置，直接使用会导致锚点记录偏移；归一化后可稳定落在文本语义位置。
- **影响范围**：`src/renderer/components/StickyView.tsx` 的 `addBlockBookmark` 位置解析逻辑与文本块优先策略。
- **状态**：待确认

## [2026-02-24/M0.13-R13-hotfix2] checkbox 行右键优先走 DOM 锚点解析

- **背景**：用户继续反馈 checkbox 行右键添加“待处理”后，回跳仍可能落到下一行末尾。
- **假设**：右键时先沿 DOM 祖先链解析 `data-node-id` 对应文档位置，优先选文本块；仅在 DOM 解析失败时才回退到 `posAtCoords`。
- **理由**：checkbox/列表符号区域属于结构边界，坐标命中不稳定；DOM 命中更贴近用户实际点击行。
- **影响范围**：`src/renderer/components/StickyView.tsx` 的 `handleContextMenu` 命中解析与选区同步逻辑。
- **状态**：待确认

## [2026-02-26/M0.13-R13-hotfix3] 容器块锚点优先下钻文本块

- **背景**：用户复测反馈右键“添加文本块到待处理”后，单击条目仍会跳到最后一行末尾，而不是添加那一行的行末。
- **假设**：当右键命中 `taskItem/taskList` 等容器块时，锚点解析统一下钻到容器内首个文本块并记录其行末偏移；仅在无文本块时再回退容器块。
- **理由**：容器块尾位置并不等价于“当前文本行末”，直接记录容器块会导致回跳偏向文档尾部；优先文本块更符合用户点击语义。
- **影响范围**：`src/renderer/components/StickyView.tsx` 的 `handleContextMenu` 命中解析、`addBlockBookmark` 锚点记录与待处理回跳落点。
- **状态**：待确认

## [2026-02-26/M0.13-R13-hotfix4] 待处理锚点写入时优先修复重复块ID

- **背景**：用户反馈在指定行右键添加待处理后，单击回跳仍跳到更靠后行，表现为“总到最末尾”。
- **假设**：重复 `data-node-id` 是本次误命中的主要来源；在添加待处理时如果命中块 ID 缺失或重复，默认重置当前块为新唯一 ID 后再写入书签。
- **理由**：回跳依赖 `blockId` 定位，ID 冲突会命中错误节点；写入时修复唯一性可从源头保证定位稳定。
- **影响范围**：`src/renderer/components/StickyView.tsx` 的待处理新增流程、`src/renderer/utils/blockScroll.ts` 的回跳命中稳定性。
- **状态**：待确认

## [2026-02-26/M0.13-R13-hotfix5] 输入聚焦期间延迟应用远端块更新

- **背景**：用户反馈输入文本时光标会偶发跳到文档末尾，直接打断连续输入。
- **假设**：本地保存后 `task:updated` 回流触发 `setContent` 是主要诱因；输入聚焦期间默认不立即应用远端块更新，而是缓存并在失焦后应用。
- **理由**：`setContent` 会重建文档并重置选区，输入中执行会导致光标跳变；延迟到失焦可兼顾编辑连续性与跨窗口同步。
- **影响范围**：`src/renderer/components/StickyView.tsx`、`src/renderer/components/TaskDetail.tsx` 的内容同步策略与焦点期行为。
- **状态**：待确认

## [2026-03-02/M0.13-R16] 待处理左右跳转默认循环（首尾相连）

- **背景**：本轮新增“底部左右按钮在待处理项中聚焦跳转”，需求未明确到达首项/末项时应禁用还是循环。
- **假设**：左右按钮采用循环跳转（在首项点“左”跳到末项，在末项点“右”跳到首项）。
- **理由**：循环模式更适合连续处理流程，减少边界停顿与回向操作成本。
- **影响范围**：`src/renderer/components/StickyView.tsx` 的 `focusPendingByDelta` 跳转规则；若改为边界禁用，仅需调整索引计算与按钮禁用状态。
- **状态**：待确认

## [2026-04-02/M0.13-R19] 回车分裂后的新行默认恢复普通优先级

- **背景**：用户反馈带优先级的列表行按下 Enter 后，下一行会自动继承同样的优先级标记，不符合“新行应为普通行”的预期。
- **假设**：本轮统一将 `priority` 属性设置为 `keepOnSplit: false`，覆盖段落、无序列表、任务列表等所有已接入优先级的块类型；即当前行保留优先级，新分裂出的下一行默认不继承。
- **理由**：优先级在当前产品里更像“本行标签”而不是“后续连续输入模式”；统一关闭继承能同时修复截图中的列表场景，并避免正文段落与任务列表出现同类问题。
- **影响范围**：`src/renderer/utils/priorityExtension.ts` 的属性拆分行为，以及所有使用 `Priority` 扩展的编辑器回车体验（`TaskDetail`、`StickyView`）。
- **状态**：待确认

## [2026-04-02/M0.13-R20] 优先级下拉继续隐藏已归档任务

- **背景**：本轮新需求明确“已完成的、删除的任务不再在优先级下拉列表中展示”，未额外说明已归档任务是否恢复展示。
- **假设**：在排除已完成和已删除任务的同时，继续保留现有的 `is_archived = 0` 过滤；也就是优先级下拉仍只展示“未完成、未删除、未归档”的任务文本块。
- **理由**：当前优先级下拉更偏向“当前待处理事项导航”；已归档任务通常已经退出活跃工作流，继续隐藏更符合已有产品语义，也避免本轮改动扩大展示范围。
- **影响范围**：`src/main/db/tasksRepo.ts` 的 `getPriorityBlocks()` 查询边界；若后续需要把已归档但未完成的任务重新纳入优先级下拉，只需调整 SQL 过滤条件与对应测试。
- **状态**：待确认
