# 项目计划（里程碑驱动）

更新时间：2026-03-16（Build-mac 已完成）

## Build-mac：mac 应用打包链路修复

### 任务清单

- [x] 执行内容：修复 Electron mac 打包配置
  - 具体执行步骤：
    1. 校正 `package.json` 中 `electron` 的依赖归类，满足 `electron-builder` 的约束
    2. 调整原生依赖安装脚本，确保 `better-sqlite3` 在 Electron 目标版本下正确重建
  - 验收标准：`electron-builder` 不再因 `electron` 所在依赖区块错误而中断

- [x] 执行内容：重新安装依赖并验证 mac 打包
  - 具体执行步骤：
    1. 运行 `npm install` 刷新锁文件与本地依赖状态
    2. 运行 `npm run build` 验证 TypeScript、Vite、Electron 打包全链路
  - 验收标准：构建完成，生成 mac 安装产物（如 `.dmg`）

- [x] 执行内容：同步项目文档
  - 具体执行步骤：
    1. 将默认技术假设写入 `ASSUMPTIONS.md`
    2. 将实现过程和结果写入 `Records.md`
    3. 更新 `PROJECT_STATUS.md` 中的构建现状
  - 验收标准：核心文档与本轮改动一致，后续会话可直接接手

状态：`[x] 已完成`

## M0.13-R18：正文标题折叠（当前窗口临时态）

### 任务清单

- [x] 执行内容：补充需求与计划文档
  - 具体执行步骤：
    1. 将“标题折叠”的交互边界写入 `PRD.md`
    2. 在 `plan.md` 新增里程碑与验收标准
  - 验收标准：文档明确“范围到下一个同级或更高级标题”“左侧小三角入口”“仅当前窗口临时生效”

- [x] 执行内容：实现正文标题折叠编辑器能力
  - 具体执行步骤：
    1. 在编辑器层新增标题折叠插件，为可折叠标题渲染左侧切换按钮
    2. 计算标题折叠范围，并在当前窗口内临时隐藏对应正文块
    3. 将能力接入 `TaskDetail` 与 `StickyView` 两个正文编辑场景
  - 验收标准：点击标题左侧小三角后，该标题下内容会隐藏到下一个同级或更高级标题；再次点击可恢复显示；切换窗口或重开应用后恢复默认展开

- [x] 执行内容：补充测试与回归验证
  - 具体执行步骤：
    1. 为标题折叠范围与临时状态切换补充单元测试
    2. 运行 `npm run test` 验证无回归
  - 验收标准：新增测试覆盖核心折叠逻辑，且 `npm run test` 全部通过

状态：`[x] 已完成`

## M0.13-R18-hotfix：标题折叠按钮可见性修复

### 任务清单

- [x] 执行内容：修复标题折叠按钮被左边界裁切
  - 具体执行步骤：
    1. 调整标题折叠按钮定位，避免使用标题外侧负偏移
    2. 为可折叠标题增加内部左边距，确保按钮和标题文字不重叠
  - 验收标准：顶格标题也能看到折叠按钮，不再因编辑区边界裁切而隐藏

- [x] 执行内容：补充标题创建方式说明并回归验证
  - 具体执行步骤：
    1. 在用户文档补充“`##` 后空格”和 `Cmd/Ctrl + Alt + 数字` 的标题输入方式
    2. 运行 `npm run test` 与渲染层 TypeScript 校验
  - 验收标准：说明能覆盖标题创建入口，且测试与类型检查通过

状态：`[x] 已完成`

## M0.13-R18-hotfix2：标题样式与折叠入口渲染修复

### 任务清单

- [x] 执行内容：将标题折叠入口改为标题节点自身渲染
  - 具体执行步骤：
    1. 去掉标题 widget 按钮实现，改为在标题节点上输出折叠属性
    2. 用标题伪元素渲染折叠三角，并仅在左侧折叠区点击时切换
  - 验收标准：标题折叠三角直接显示在标题行内部，不再依赖额外按钮节点位置

- [x] 执行内容：补齐标题层级样式
  - 具体执行步骤：
    1. 为 `h1~h6` 增加字号、字重、行高和间距
    2. 验证标题与正文有明显视觉差异
  - 验收标准：标题不再与正文同字号同字重，层级感清晰

- [x] 执行内容：执行测试与类型校验
  - 具体执行步骤：
    1. 补充装饰属性测试
    2. 运行 `npm run test` 与 `npx tsc -p tsconfig.renderer.json --noEmit`
  - 验收标准：测试与类型检查通过

状态：`[x] 已完成`

## M0.13-R18-hotfix3：标题折叠真实 DOM 同步修复

### 任务清单

- [x] 执行内容：修复标题折叠入口未真正挂到标题 DOM 的问题
  - 具体执行步骤：
    1. 修正标题区间位置计算，改用 ProseMirror 真实节点位置
    2. 将标题折叠入口与隐藏状态改为同步到真实 DOM 类名与 `data-*` 属性
    3. DOM 同步时暂停并恢复 ProseMirror DOM observer，避免观察器反复回流
  - 验收标准：标题元素真实带有 `data-heading-*` 和 `heading-collapsible`，折叠后三角和隐藏样式都能命中

- [x] 执行内容：补充真实 DOM 回归测试
  - 具体执行步骤：
    1. 在测试中断言标题 DOM 上实际出现 `data-heading-collapsible`
    2. 在测试中断言折叠后正文块出现 `heading-collapsed-content`
  - 验收标准：`npm run test` 通过，真实 DOM 测试覆盖初始化与折叠后两种场景

状态：`[x] 已完成`

## Build-dev-hotfix：开发环境固定 Vite 端口并等待 renderer ready

### 任务清单

- [x] 执行内容：修复 Electron dev 链路可能连错旧 Vite 服务的问题
  - 具体执行步骤：
    1. 将 Vite dev server 设为 `strictPort: true`
    2. 将 `npm run dev` 中 Electron 启动前置条件补为等待 `tcp:5173`
  - 验收标准：当 `5173` 被占用时，Vite 直接失败而不是偷偷切到 `5174`；Electron 只会在 `5173` ready 后启动

状态：`[x] 已完成`

## M0.13-R18-hotfix4：重复标题节点 ID 下的独立折叠修复

### 任务清单

- [x] 执行内容：修复多个标题共享同一节点 ID 时会一起折叠的问题
  - 具体执行步骤：
    1. 将标题折叠状态键从单纯 `headingId` 改为 `headingKey`
    2. `headingKey` 采用“标题节点 ID + 同 ID 出现序号”的组合方式生成
    3. DOM 点击和折叠状态同步统一改为使用 `headingKey`
  - 验收标准：同一页面内即使多个标题意外共享相同 `data-node-id`，每个标题仍可独立折叠/展开

- [x] 执行内容：补充重复 ID 场景测试并回归验证
  - 具体执行步骤：
    1. 新增“重复标题节点 ID”测试，验证两个标题不会被视为同一折叠目标
    2. 运行 `npm run test` 和 `npx tsc -p tsconfig.renderer.json --noEmit`
  - 验收标准：测试和类型检查通过

状态：`[x] 已完成`

## 里程碑总览

- [x] M0：文档基线对齐（PRD/计划/假设/决策）
- [x] M0.5：里程碑 Checkpoint 自动化（日志/截图/变更快照）
- [x] M0.6：Session Release Skill（总结/提交/截图/飞书）
- [x] M0.7：Session Release 飞书消息联调测试（仅通知链路）
- [x] M0.8：Session Release 精简流改造（AI摘要/提交/飞书）
- [x] M0.9：功能补充建议文档（基于现状评估）
- [x] M0.10：P1 建议执行任务拆解（文档化排期）
- [x] M0.11：同父任务便签状态共享与路径化打开
- [x] M0.12：子任务交互增强（`Docs/NEW_FEATURES.md` #1.1~1.3）
- [x] M0.12-R1：子任务完成态实时同步修复（latest-write-wins）
- [x] M0.12-R2：重命名后 taskLink 样式保留修复
- [x] M0.12-R3：taskLink checkbox 视觉对齐系统默认样式
- [x] M0.12-R4：便签根任务书签可移除修复
- [x] M0.13：子任务交互扩展（`Docs/NEW_FEATURES.md` #1.4~1.7）
- [x] M0.13-R1：插入已有子任务下拉化 + 归档入口迁移
- [x] M0.13-R2：兄弟级下拉过滤已删除/已归档任务
- [x] M0.13-R3：便签插入已有子任务改右键 + 未引用过滤
- [x] M0.13-R4：任务树超长列表滚动修复
- [x] M0.13-R5：右键插入子任务二级菜单 + 菜单防截断
- [x] M0.13-R5-hotfix：右键菜单黑屏修复（Hook 顺序）
- [x] M0.13-R6：右键菜单独立弹窗化（防窗口裁切）
- [x] M0.13-R7：`Ctrl/Cmd+Shift+T` 无选中按当前行转子任务
- [x] M0.13-R8：hover 便签改为显示层级路径
- [x] M0.13-R9：便签“待处理”按钮点击无响应修复
- [x] M0.13-R10：待处理弹层样式适配 + 点击定位与光标落点
- [x] M0.13-R11：同页多文本块可重复加入“待处理”
- [x] M0.13-R12：任务重名规则改为“同父任务兄弟子任务唯一”
- [x] M0.13-R13：待处理右键记录光标锚点改为对应行末
- [x] M0.13-R13-hotfix：列表符号/块边界右键锚点偏移修复
- [x] M0.13-R13-hotfix2：checkbox 行右键锚点 DOM 优先解析
- [x] M0.13-R13-hotfix3：容器块命中导致回跳到文末修复
- [x] M0.13-R13-hotfix4：重复块ID导致待处理回跳误命中修复
- [x] M0.13-R13-hotfix5：输入时光标偶发跳到文末修复
- [x] M0.13-R14：面包屑/待处理中隐藏已完成与已删除任务
- [x] M0.13-R15：待处理增加序号并支持拖拽排序
- [x] M0.13-R16：待处理聚焦模式与底部左右跳转
- [x] M0.13-R17：便签右键增加“显示/隐藏已打钩 checkbox 文本块”
- [x] M0.13-R18：正文标题折叠（当前窗口临时态）
- [x] M0.13-R18-hotfix：标题折叠按钮可见性修复
- [x] M0.13-R18-hotfix2：标题样式与折叠入口渲染修复
- [x] M0.13-R18-hotfix3：标题折叠真实 DOM 同步修复
- [x] M0.13-R18-hotfix4：重复标题节点 ID 下的独立折叠修复
- [x] Build-dev-hotfix：开发环境固定 Vite 端口并等待 renderer ready
- [x] M0.13-Docs：面向用户的使用帮助文档补充完善
- [ ] M3：Markdown 粘贴解析能力补齐

---

## M0.13-R17：便签右键增加“显示/隐藏已打钩 checkbox 文本块”

### 任务清单

- [x] 执行内容：在 `StickyView` 右键菜单增加显示/隐藏切换项
  - 验收标准：右键菜单可看到“显示已打钩checkbox文本块/隐藏已打钩checkbox文本块”，点击后状态可切换

- [x] 执行内容：在便签编辑区按开关状态隐藏已打钩任务项
  - 验收标准：开关关闭时，`taskList` 中 `data-checked=true` 的文本块不显示；重新开启后恢复显示

- [x] 执行内容：回归验证
  - 验收标准：`npm run test` 通过，无已有单测回归失败

状态：`[x] 已完成`

---

## M0：文档基线对齐（已完成）

### 任务清单

- [x] 执行内容：基于当前代码盘点已实现能力与差距
  - 验收标准：`PRD.md` 中“已实现/部分实现/未实现”与代码一致

- [x] 执行内容：建立根目录四文档治理结构
  - 验收标准：根目录存在 `PRD.md`、`plan.md`、`ASSUMPTIONS.md`、`DECISIONS.md`

- [x] 执行内容：输出下一阶段可执行里程碑拆分
  - 验收标准：每个里程碑可在 2–5 分钟内进行一次验收确认

状态：`[x] 已完成`

---

## M0.5：里程碑 Checkpoint 自动化（当前里程碑）

### 任务清单

- [x] 执行内容：提供可复用的 checkpoint 脚本
  - 验收标准：支持 `npm run checkpoint -- <里程碑名>` 生成 `checkpoints/<里程碑>_<时间戳>/`

- [x] 执行内容：沉淀里程碑日志约定
  - 验收标准：自动追加 `checkpoints/log.md`，包含构建结果、截图状态、摘要路径

- [x] 执行内容：补齐截图依赖安装入口
  - 验收标准：可通过 `npm run checkpoint:setup` 一键安装 Playwright + Chromium

状态：`[x] 已完成`

---

## M0.6：Session Release Skill（总结/提交/截图/飞书）

### 任务清单

- [x] 执行内容：创建新 skill 骨架并完成触发描述
  - 验收标准：存在 `~/.codex/skills/session-release-bot/`，且 `SKILL.md`、`agents/openai.yaml` 可用

- [x] 执行内容：实现会话收尾自动化脚本
  - 验收标准：脚本支持“改动汇总、Git 提交、启动 `npm run dev`、界面截图、飞书机器人发送”主流程

- [x] 执行内容：补充参考文档与结构校验
  - 验收标准：存在 `references/usage.md`、`references/feishu.md`，且 `quick_validate.py` 校验通过

状态：`[x] 已完成`

---

## M0.7：Session Release 飞书消息联调测试（仅通知链路）

### 任务清单

- [x] 执行内容：按 skill 脚本触发一次飞书通知测试
  - 验收标准：`release-summary.json` 中 `feishu_result.status` 为 `ok`

- [x] 执行内容：沉淀测试结论与产物路径
  - 验收标准：`DECISIONS.md` 新增 M0.7 记录，包含报告与摘要路径

状态：`[x] 已完成`

---

## M0.8：Session Release 精简流改造（AI摘要/提交/飞书）

### 任务清单

- [x] 执行内容：改造脚本支持自动加载 `.env`
  - 验收标准：不手动 `source .env` 时也可读取 `FEISHU_BOT_WEBHOOK`

- [x] 执行内容：裁剪流程与产物，仅保留 AI 摘要、提交、飞书通知
  - 验收标准：默认不再执行 dev/截图，输出不再包含无关日志文件

- [x] 执行内容：执行一次真实链路验证并沉淀结论
  - 验收标准：`release-summary.json` 中 commit 与 feishu 状态符合预期

状态：`[x] 已完成`

---

## M0.9：功能补充建议文档（基于现状评估）

### 任务清单

- [x] 执行内容：审查当前实现能力与界面信息
  - 验收标准：建议项不重复已完整上线能力，且明确与现有模块衔接点

- [x] 执行内容：输出“尽量全面”的功能补充建议文档
  - 验收标准：建议按模块分组并给出优先级/价值描述，覆盖核心流程、效率提升、稳定性、长期演进

- [x] 执行内容：沉淀阶段化落地建议
  - 验收标准：文档包含 1~3 期可执行推进顺序，便于后续进入开发计划

状态：`[x] 已完成`

---


## M0.10：P1 建议执行任务拆解（文档化排期）

### 任务清单

- [x] 执行内容：将 P1 建议逐条映射为开发执行任务
  - 验收标准：每条建议都有“执行任务 + 验收关键点”映射

- [x] 执行内容：按里程碑分组形成可执行迭代路线
  - 验收标准：至少拆分 4 个里程碑，且每个里程碑具备 checklist 与验收标准

- [x] 执行内容：沉淀实施依赖与风险提示
  - 验收标准：明确数据迁移、交互复杂度、性能与一致性风险

状态：`[x] 已完成`

---

## M0.11：同父任务便签状态共享与路径化打开

### 任务清单

- [x] 执行内容：调整 sticky 打开策略，统一归属到顶层父任务
  - 验收标准：从任意子任务右键“打开置顶便签”时，窗口根任务统一归属于顶层父任务

- [x] 执行内容：保持“打开位置”语义，按目标任务构建导航路径
  - 验收标准：在便签中右键“在新便签中打开”时，新便签默认落在被打开任务页面

- [x] 执行内容：同步同父任务便签共享状态（书签/皮肤）
  - 验收标准：同一顶层父任务下多便签修改书签、颜色、透明度时可实时互相同步

状态：`[x] 已完成`

---

## M0.12：子任务交互增强（`Docs/NEW_FEATURES.md` #1.1~1.3）

### 任务清单

- [x] 执行内容：链接块保持内联形态并增强为卡片化 TaskItem 样式（`Library` / `Sticky` 一致）
  - 具体执行步骤：
    1. 扩展 `taskLink` 节点属性，承载完成态展示信息
    2. 调整渲染与样式，使链接块具备卡片边框、checkbox、完成划线
    3. 统一 `TaskDetail` 与 `StickyView` 的交互行为
  - 验收标准：链接块视觉为“内联卡片 + checkbox”，在两个窗口中表现一致

- [x] 执行内容：实现链接块 checkbox 与任务树/详情完成状态双向同步
  - 具体执行步骤：
    1. 在编辑器内勾选链接块时触发 `task:update(isCompleted)`
    2. 监听任务变更时回写并刷新正文中对应链接块完成态
    3. 保持现有任务树 checkbox 行为不回退
  - 验收标准：任一入口切换完成状态后，任务树与正文链接块均保持同步
  - 依赖关系：依赖“链接块卡片化”任务

- [x] 执行内容：实现 markdown checkbox 与子任务完成状态双向同步（仅关联项生效）
  - 具体执行步骤：
    1. 建立“markdown checkbox ↔ 子任务”关联映射（链接块优先 + 文本匹配）
    2. 在正文勾选关联 checkbox 时同步更新子任务完成态
    3. 在任务完成态变化时回写关联 checkbox 文本
  - 验收标准：关联项双向同步；无关联普通 checkbox 保持原样
  - 依赖关系：依赖“链接块卡片化”与“双向同步”任务

- [x] 执行内容：实现子任务标题重命名增强（`Library` 顶栏 + `Sticky` 顶栏）及引用同步
  - 具体执行步骤：
    1. 支持在 `TaskDetail` 顶部标题栏直接编辑
    2. 支持在 `Sticky` 顶部标题栏直接编辑
    3. 重命名后同步更新 `taskLink` 标题、已关联 markdown checkbox 文本、sticky 书签标题
  - 验收标准：两个入口均可重命名，所有引用位置实时更新
  - 依赖关系：依赖“markdown checkbox 同步”任务

- [x] 执行内容：新增子任务重名约束与用户提示（不支持同名子任务）
  - 具体执行步骤：
    1. 在子任务创建与重命名链路增加重名校验
    2. 对冲突场景提供前端提示并阻止写入
    3. 保证不会产生新的重名子任务
  - 验收标准：重复名称提交失败且有可理解提示
  - 依赖关系：依赖“重命名增强”任务

- [x] 执行内容：补充测试与文档同步（`PRD.md` / `ASSUMPTIONS.md` / `Records.md` / `PROJECT_STATUS.md`）
  - 具体执行步骤：
    1. 为新增解析与同步逻辑补充单元测试
    2. 运行 `npm run test` 与必要构建检查
    3. 完成文档更新与交付报告
  - 验收标准：测试通过，核心文档与代码一致
  - 依赖关系：依赖以上全部任务

状态：`[x] 已完成`

---

## M0.12-R1：子任务完成态实时同步修复（latest-write-wins）

### 任务清单

- [x] 执行内容：修复 `loadTask` / `refreshLibrary` 并发竞态，保证晚到响应不覆盖最新状态
  - 验收标准：快速连续勾选/反勾选时，任务树始终反映最后一次用户操作结果

- [x] 执行内容：移除编辑器焦点状态下的 blocks 回写阻断，保证跨窗口完成态即时回显
  - 验收标准：在 `TaskDetail` / `StickyView` 聚焦编辑时，来自任务树或其他窗口的完成态变化可立即渲染

- [x] 执行内容：完成回归验证并同步文档记录
  - 验收标准：`tsc` 与 `npm run test` 通过，`PRD.md`/`Records.md`/`PROJECT_STATUS.md` 同步更新

状态：`[x] 已完成`

---

## M0.12-R2：重命名后 taskLink 样式保留修复

### 任务清单

- [x] 执行内容：修复重命名同步逻辑，避免将 `taskItem` 内 `taskLink` 替换为纯文本
  - 验收标准：在任务树重命名子任务后，父任务正文中对应链接块仍保持 taskLink 卡片样式

- [x] 执行内容：补齐回归测试，覆盖“taskItem 内嵌 taskLink 的重命名”场景
  - 验收标准：新增测试通过，断言节点类型仍为 `taskLink` 且标题已更新

- [x] 执行内容：同步文档记录并完成回归验证
  - 验收标准：`tsc` 与 `npm run test` 通过，`PRD.md`/`Records.md`/`PROJECT_STATUS.md` 已更新

状态：`[x] 已完成`

---

## M0.12-R3：taskLink checkbox 视觉对齐系统默认样式

### 任务清单

- [x] 执行内容：移除 taskLink checkbox 的自绘样式，改为系统默认 checkbox 外观
  - 验收标准：checkbox 不再出现黑色背景与白色勾选符号，视觉与普通 checkbox 一致

- [x] 执行内容：保持 `Library` 与 `Sticky` 双场景样式一致
  - 验收标准：两个界面中的 taskLink checkbox 外观一致，且勾选交互正常

- [x] 执行内容：完成回归验证并同步文档记录
  - 验收标准：`npm run test` 通过，`plan.md` / `Records.md` / `PROJECT_STATUS.md` 已更新

状态：`[x] 已完成`

---

## M0.12-R4：便签根任务书签可移除修复

### 任务清单

- [x] 执行内容：修复便签书签栏中“默认父任务书签无法删除”的问题
  - 验收标准：点击父任务书签 `×` 后该项可被正常移除，且不会被立即自动加回

- [x] 执行内容：保留“首次默认展示父任务书签”体验
  - 验收标准：新建/首次打开便签时仍默认显示父任务书签

- [x] 执行内容：完成回归验证并同步文档记录
  - 验收标准：`npm run test` 通过，`plan.md` / `Records.md` / `PROJECT_STATUS.md` 已更新

状态：`[x] 已完成`

---

## M0.13：子任务交互扩展（`Docs/NEW_FEATURES.md` #1.4~1.7）

### 任务清单

- [x] 执行内容：实现“插入已有子任务链接”入口（覆盖 1.4）
  - 具体执行步骤：
    1. 在 `TaskDetail` / `StickyView` 编辑区右键菜单增加“插入已有子任务链接”操作
    2. 复用现有标题输入能力，增加“从当前父任务子节点中选择已有任务”流程
    3. 选中后在当前光标位置插入 `taskLink` 节点，不新建任务
  - 验收标准：删除链接块后可通过右键重新插入同一子任务链接，入口恢复正常

- [x] 执行内容：补齐树面板拖拽改层级能力（覆盖 1.5）
  - 具体执行步骤：
    1. 在 `LibraryPanel` 为任务节点增加拖拽源与放置区域（同级前后、作为子级）
    2. 主进程新增 `task:reparent` IPC，统一处理父子关系变更与排序落库
    3. 拖拽完成后刷新树结构并保持当前选中任务与导航路径一致
  - 验收标准：拖拽后任务层级即时更新，重启后层级保持一致
  - 依赖关系：依赖“数据层支持父子关系重排”

- [x] 执行内容：实现“移动子任务引用到其它父任务”（覆盖 1.6）
  - 具体执行步骤：
    1. 在 `taskLink` 右键菜单增加“移动到...”操作
    2. 选择目标父任务后执行原子操作：更新 `edges` 父子关系、删除原父任务中的链接块、向目标父任务插入新链接块
    3. 移动完成后广播刷新原父/目标父相关窗口与书签展示
  - 验收标准：移动后原位置链接消失、目标位置出现新链接，任务本体不丢失
  - 依赖关系：依赖“树层级变更能力（1.5）”的边关系更新基础

- [x] 执行内容：实现“一键归档已完成子任务”（覆盖 1.7）
  - 具体执行步骤：
    1. 在父任务详情页增加“归档已完成子任务”按钮与确认提示
    2. 主进程新增批量归档 IPC：筛选当前父任务下已完成子任务并执行归档
    3. 归档后同步清理父任务正文中的对应 `taskLink` 引用，保留库面板可检索能力
  - 验收标准：点击后仅已完成子任务被归档，正文链接同步移除，搜索仍可找到归档任务
  - 依赖关系：依赖“taskLink ↔ 子任务映射能力（M0.12 已完成）”

- [x] 执行内容：补齐测试与回归验证
  - 具体执行步骤：
    1. 增加 `taskBlocksSync` / IPC 层单测：覆盖重插入、拖拽改层级、移动引用、批量归档
    2. 回归验证 `Library` / `Sticky` 一致性与多窗口同步
    3. 运行 `npm run test` 并记录关键人工验证用例
  - 验收标准：新增测试通过，核心路径人工验证通过，无已完成功能回退
  - 依赖关系：依赖以上全部任务

- [x] 执行内容：同步文档与交付记录
  - 具体执行步骤：
    1. 更新 `PRD.md`（如实现边界有落地调整）
    2. 更新 `ASSUMPTIONS.md`、`Records.md`、`PROJECT_STATUS.md`
    3. 输出阶段交付报告
  - 验收标准：文档与实现一致，后续会话可直接接手
  - 依赖关系：依赖“测试与回归验证”任务

状态：`[x] 已完成`

---

## M0.13-R1：插入已有子任务下拉化 + 归档入口迁移

### 任务清单

- [x] 执行内容：将“插入已有子任务链接”改为顶部下拉选择（详情页 + 便签）
  - 验收标准：不再使用输入序号方式；两个界面均可通过下拉直接选择并插入目标子任务链接

- [x] 执行内容：移除任务详情页顶部“归档已完成子任务”按钮
  - 验收标准：详情页顶部不再显示该按钮

- [x] 执行内容：将“归档已完成子任务”迁移到任务树右键菜单
  - 验收标准：任务树右键菜单可触发归档流程，完成后刷新树与详情

- [x] 执行内容：回归验证并同步文档
  - 验收标准：`tsc` 与 `npm run test` 通过，`plan.md` / `Records.md` / `PROJECT_STATUS.md` 已更新

状态：`[x] 已完成`

---

## M0.13-R2：兄弟级下拉过滤已删除/已归档任务

### 任务清单

- [x] 执行内容：调整兄弟级下拉数据源过滤规则
  - 验收标准：面包屑兄弟级下拉中不再出现已删除/已归档任务

- [x] 执行内容：回归验证并同步文档
  - 验收标准：`tsc` 与 `npm run test` 通过，`plan.md` / `Records.md` / `PROJECT_STATUS.md` 已更新

状态：`[x] 已完成`

---

## M0.13-R3：便签插入已有子任务改右键 + 未引用过滤

### 任务清单

- [x] 执行内容：移除便签顶部“插入已有子任务”按钮
  - 验收标准：`Sticky` 头部控件区不再显示“插入已有子任务”下拉按钮

- [x] 执行内容：将便签“插入已有子任务”迁移为右键菜单
  - 验收标准：在便签编辑区右键可看到“插入子任务：xxx”菜单项并可直接插入对应子任务链接

- [x] 执行内容：插入候选仅显示“未被正文引用”的子任务
  - 验收标准：已存在 `taskLink` 引用的子任务不会出现在插入候选中

- [x] 执行内容：回归验证并同步文档
  - 验收标准：`tsc` 与 `npm run test` 通过，`PRD.md` / `plan.md` / `Records.md` / `PROJECT_STATUS.md` 已更新

状态：`[x] 已完成`

---

## M0.13-R4：任务树超长列表滚动修复

### 任务清单

- [x] 执行内容：修复任务树超出面板高度后不可完整查看的问题
  - 验收标准：任务树区域内容超高时可通过滚动完整浏览所有节点

- [x] 执行内容：为任务树增加可见滚动条样式
  - 验收标准：任务树区域出现细滚动条，滚动交互可用且不遮挡主要内容

- [x] 执行内容：修复布局最小高度约束，确保滚动容器正确收缩
  - 验收标准：`LibraryPanel` 在主布局中不再因 flex 最小高度导致溢出裁切

- [x] 执行内容：回归验证并同步文档
  - 验收标准：`tsc` 与 `npm run test` 通过，`PRD.md` / `plan.md` / `Records.md` / `PROJECT_STATUS.md` 已更新

状态：`[x] 已完成`

---

## M0.13-R5：右键插入子任务二级菜单 + 菜单防截断

### 任务清单

- [x] 执行内容：将 sticky 右键“插入子任务”改为二级展开菜单
  - 验收标准：右键菜单先显示“插入子任务”，点击后再展示子任务列表，而非直接平铺“插入子任务：xxx”

- [x] 执行内容：右键菜单支持长列表内部滚动
  - 验收标准：菜单超出可视高度时可在菜单内部滚动查看全部项，不再被便签窗口裁切

- [x] 执行内容：补齐菜单组件类型与样式能力
  - 验收标准：`ContextMenu` 支持 `children/disabled`，并通过编译检查

- [x] 执行内容：回归验证并同步文档
  - 验收标准：`tsc` 与 `npm run test` 通过，`PRD.md` / `plan.md` / `Records.md` / `PROJECT_STATUS.md` / `ASSUMPTIONS.md` 已更新

状态：`[x] 已完成`

---

## M0.13-R5-hotfix：右键菜单黑屏修复（Hook 顺序）

### 任务清单

- [x] 执行内容：修复 `ContextMenu` 的 Hook 调用顺序问题
  - 验收标准：不再出现 “Rendered more hooks than during the previous render” 报错

- [x] 执行内容：完成回归验证
  - 验收标准：`npm run test` 与 `tsc`（main/preload/renderer）通过，右键菜单正常渲染

- [x] 执行内容：同步文档与交付记录
  - 验收标准：`PRD.md` / `plan.md` / `Records.md` / `PROJECT_STATUS.md` 已更新

状态：`[x] 已完成`

---

## M0.13-R6：右键菜单独立弹窗化（防窗口裁切）

### 任务清单

- [x] 执行内容：将 sticky 右键菜单从宿主渲染层浮层迁移为独立窗口
  - 验收标准：右键菜单由独立 popup 窗口承载，显示范围不受便签窗口边界裁切

- [x] 执行内容：打通菜单数据传递与点击回传链路
  - 验收标准：主窗口可发送菜单树到弹窗，弹窗点击项可回传主窗口执行对应动作

- [x] 执行内容：保持现有二级菜单与空候选禁用提示能力
  - 验收标准：`插入子任务` 二级展开与“暂无可选子任务”禁用态表现不回退

- [x] 执行内容：回归验证并同步文档
  - 验收标准：`npm run test` 与 `tsc`（main/preload/renderer）通过，文档已更新

状态：`[x] 已完成`

---

## M0.13-R7：`Ctrl/Cmd+Shift+T` 无选中按当前行转子任务

### 任务清单

- [x] 执行内容：补齐快捷键无选区行为
  - 验收标准：在光标态触发 `Ctrl/Cmd+Shift+T` 时，自动提取当前行文本并转换为子任务

- [x] 执行内容：保持选区行为与单行限制兼容
  - 验收标准：有选区时仍按选中文本转换；跨多行仍提示“只能转换单行文本”

- [x] 执行内容：同步文档与回归验证
  - 验收标准：`plan.md` / `Records.md` / `PROJECT_STATUS.md` 已更新，`npm run test` 通过

状态：`[x] 已完成`

---

## M0.13-R8：hover 便签改为显示层级路径

### 任务清单

- [x] 执行内容：调整便签书签 hover 提示文案
  - 验收标准：hover 时显示“从最父一级的下一级到所在页面”的路径，格式为 `xx/xxx`

- [x] 执行内容：加入路径查询的缓存与兜底
  - 验收标准：同一书签重复 hover 不重复请求；接口异常时可回退显示标题

- [x] 执行内容：同步文档与回归验证
  - 验收标准：`plan.md` / `Records.md` / `PROJECT_STATUS.md` 已更新，`npm run test` 通过

状态：`[x] 已完成`

---

## M0.13-R9：便签“待处理”按钮点击无响应修复

### 任务清单

- [x] 执行内容：定位“待处理”按钮点击无响应根因
  - 验收标准：明确复现路径与触发链路，确认问题由前端事件处理导致

- [x] 执行内容：修复按钮点击后弹层被立即关闭的问题
  - 验收标准：点击“待处理（n）”可稳定展开列表，再次点击可收起

- [x] 执行内容：回归验证并同步文档
  - 验收标准：`npm run test` 通过，`plan.md` / `Records.md` / `PROJECT_STATUS.md` 已更新

状态：`[x] 已完成`

---

## M0.13-R10：待处理弹层样式适配 + 点击定位与光标落点

### 任务清单

- [x] 执行内容：调整“待处理”弹层样式与便签背景适配
  - 验收标准：弹层背景、边框、hover 反馈与便签主题一致，不再出现突兀白底

- [x] 执行内容：修复点击条目后的定位与光标位置
  - 验收标准：点击任一待处理条目后可定位到对应块，并将光标落在该行末尾

- [x] 执行内容：回归验证并同步文档
  - 验收标准：`npm run test` 通过，`plan.md` / `Records.md` / `PROJECT_STATUS.md` / `ASSUMPTIONS.md` 已更新

状态：`[x] 已完成`

---

## M0.13-R11：同页多文本块可重复加入“待处理”

### 任务清单

- [x] 执行内容：修复“添加文本块到待处理”的定位来源
  - 验收标准：右键任意文本块后执行“添加文本块到待处理”，应以右键点击块为准，而非旧光标所在块

- [x] 执行内容：补齐块 ID 写回事务，避免类型与命令链冲突
  - 验收标准：块无 `id` 时可正常写回 `id`，`tsc` 不再出现 `setNodeMarkup` 链式命令类型报错

- [x] 执行内容：回归验证并同步文档
  - 验收标准：`npm run test` 与 `npx tsc -p tsconfig.main.json && npx tsc -p tsconfig.preload.json && npx tsc -p tsconfig.renderer.json` 通过，`PRD.md` / `plan.md` / `Records.md` / `PROJECT_STATUS.md` / `ASSUMPTIONS.md` 已更新

状态：`[x] 已完成`

---

## M0.13-R12：任务重名规则改为“同父任务兄弟子任务唯一”

### 任务清单

- [x] 执行内容：将重名校验从“全库唯一”改为“同父任务下兄弟子任务唯一”
  - 验收标准：跨父任务允许同名；同一父任务下创建/重命名子任务时仍拦截重名

- [x] 执行内容：补齐子任务挂接入口的同父级重名防护
  - 验收标准：插入已有子任务、移动子任务引用、`edge:create/reparent` 到目标父任务时，若与目标父任务现有兄弟同名则阻止操作

- [x] 执行内容：回归验证并同步文档
  - 验收标准：`npm run test` 与 `npx tsc -p tsconfig.main.json --noEmit && npx tsc -p tsconfig.preload.json --noEmit && npx tsc -p tsconfig.renderer.json --noEmit` 通过，`PRD.md` / `plan.md` / `Records.md` / `PROJECT_STATUS.md` / `ASSUMPTIONS.md` / `Docs/用户指南.md` 已更新

状态：`[x] 已完成`

---

## M0.13-Docs：面向用户的使用帮助文档补充完善

### 任务清单

- [x] 执行内容：核对当前实现与既有《用户指南》的功能描述一致性
  - 验收标准：窗口类型、入口位置、菜单文案、快捷键描述与代码实现一致

- [x] 执行内容：重构《用户指南》结构并补充高频场景说明
  - 验收标准：文档包含“快速上手、窗口协作、任务拆分、导航与专注、推荐工作流、FAQ”等面向终端用户章节

- [x] 执行内容：同步治理文档与执行假设
  - 验收标准：`plan.md` / `Records.md` / `PROJECT_STATUS.md` / `ASSUMPTIONS.md` 已更新且记录本次文档范围假设

状态：`[x] 已完成`

---

## M0.13-R13：待处理右键记录光标锚点改为对应行末

### 任务清单

- [x] 执行内容：记录“添加文本块到待处理”时的行末光标偏移
  - 验收标准：在 sticky 右键执行“添加文本块到待处理”后，待处理条目保存对应行末偏移信息，而非仅保存块 ID

- [x] 执行内容：点击待处理条目时优先恢复记录偏移
  - 验收标准：点击待处理条目后，光标优先落在记录时对应行末；历史数据无偏移时仍可按块末尾兜底定位

- [x] 执行内容：更新书签持久化解析与文档
  - 验收标准：`WindowBookmark`、`windowStateRepo` 与 `PRD.md` / `plan.md` / `Records.md` / `PROJECT_STATUS.md` / `ASSUMPTIONS.md` 完整同步

状态：`[x] 已完成`

---

## M0.13-R13-hotfix：列表符号/块边界右键锚点偏移修复

### 任务清单

- [x] 执行内容：归一化右键命中位置到最近可编辑文本位置
  - 验收标准：在列表符号区、块边界右键“添加文本块到待处理”后，记录的锚点落在对应文本块，而非父块边界位置

- [x] 执行内容：优先按文本块记录行末偏移
  - 验收标准：待处理条目回跳时，光标落在被记录行末，不偏移到下一行中间

- [x] 执行内容：回归验证并同步文档
  - 验收标准：`npm run test` 与三端 `tsc --noEmit` 通过，`PRD.md` / `plan.md` / `Records.md` / `PROJECT_STATUS.md` / `ASSUMPTIONS.md` 已更新

状态：`[x] 已完成`

---

## M0.13-R13-hotfix2：checkbox 行右键锚点 DOM 优先解析

### 任务清单

- [x] 执行内容：右键命中位置优先从 DOM 祖先节点解析文本块锚点
  - 验收标准：在 checkbox 行（含列表符号区域）右键“添加文本块到待处理”后，记录锚点指向当前文本行而非相邻行

- [x] 执行内容：补齐边界场景的选择归一化策略
  - 验收标准：当 DOM 无法直接解析时，退回 `posAtCoords` 并使用左偏 `Selection.near(..., -1)` 归一化，避免落到下一行

- [x] 执行内容：回归验证并同步文档
  - 验收标准：`npm run test` 与三端 `tsc --noEmit` 通过，`PRD.md` / `plan.md` / `Records.md` / `PROJECT_STATUS.md` / `ASSUMPTIONS.md` 已更新

状态：`[x] 已完成`

---

## M0.13-R13-hotfix3：容器块命中导致回跳到文末修复

### 任务清单

- [x] 执行内容：统一右键命中解析策略，容器块下钻到文本块
  - 验收标准：在 checkbox / `taskItem` / `taskList` 等容器区域右键添加待处理时，记录锚点对应当前文本行

- [x] 执行内容：添加待处理时优先记录文本块行末，不再记录容器块尾
  - 验收标准：单击待处理条目后，光标落到“添加那一行”行末，不再跳到最后一行末尾

- [x] 执行内容：回归验证并同步文档
  - 验收标准：`npm run test` 与三端 `tsc --noEmit` 通过，`PRD.md` / `plan.md` / `Records.md` / `PROJECT_STATUS.md` / `ASSUMPTIONS.md` 已更新

状态：`[x] 已完成`

---

## M0.13-R13-hotfix4：重复块ID导致待处理回跳误命中修复

### 任务清单

- [x] 执行内容：添加待处理时校验并修复目标块 ID 唯一性
  - 验收标准：若目标块 ID 缺失或重复，记录前自动为当前块重置唯一 ID，避免与其他块冲突

- [x] 执行内容：回跳查找命中后不再被后续同 ID 节点覆盖
  - 验收标准：存在历史重复 ID 时，回跳结果稳定且不再“越过目标跳到更后位置”

- [x] 执行内容：回归验证并同步文档
  - 验收标准：`npm run test` 与三端 `tsc --noEmit` 通过，`PRD.md` / `plan.md` / `Records.md` / `PROJECT_STATUS.md` / `ASSUMPTIONS.md` 已更新

状态：`[x] 已完成`

---

## M0.13-R13-hotfix5：输入时光标偶发跳到文末修复

### 任务清单

- [x] 执行内容：识别并拦截“本地保存回流”触发的编辑器重设
  - 验收标准：输入过程中收到同窗口 `task:update` 回流时，不再触发 `setContent` 重置光标

- [x] 执行内容：聚焦输入期间延迟应用远端内容
  - 验收标准：正在输入时如收到非本地回流更新，不打断输入；失焦后再应用远端内容

- [x] 执行内容：双编辑器（Library/Sticky）一致性修复与回归
  - 验收标准：`TaskDetail` 与 `StickyView` 同步修复，`npm run test` 与三端 `tsc --noEmit` 通过

- [x] 执行内容：同步文档与假设记录
  - 验收标准：`PRD.md` / `plan.md` / `Records.md` / `PROJECT_STATUS.md` / `ASSUMPTIONS.md` 已更新

状态：`[x] 已完成`

---

## M0.13-R14：面包屑/待处理中隐藏已完成与已删除任务

### 任务清单

- [x] 执行内容：调整面包屑同级下拉过滤规则，排除已完成任务
  - 验收标准：面包屑下拉列表中不再出现 `isCompleted=true` 或 `isDeleted=true` 的同级任务

- [x] 执行内容：调整 sticky 待处理条目可见性，排除已完成/已删除任务来源
  - 验收标准：待处理按钮计数与弹层列表均不展示来源任务已完成或已删除的条目

- [x] 执行内容：补齐事件触发后的可见性刷新与回归验证
  - 验收标准：任务状态变更（完成/删除）后待处理列表自动刷新；`npm run test` 与三端 `tsc --noEmit` 通过

状态：`[x] 已完成`

---

## M0.13-R15：待处理增加序号并支持拖拽排序

### 任务清单

- [x] 执行内容：为待处理弹层条目增加序号展示
  - 验收标准：待处理列表每条记录都显示稳定序号，顺序与当前展示一致

- [x] 执行内容：实现待处理条目拖拽排序并写回持久状态
  - 验收标准：拖拽后列表顺序即时更新；关闭弹层或窗口后再次打开顺序保持

- [x] 执行内容：补齐拖拽视觉反馈与回归验证
  - 验收标准：拖拽中有明确视觉状态（拖动项/目标项）；`npm run test` 与三端 `tsc --noEmit` 通过

状态：`[x] 已完成`

---

## M0.13-R16：待处理聚焦模式与底部左右跳转

### 任务清单

- [x] 执行内容：点击待处理条目时进入“当前项聚焦”状态
  - 验收标准：点击待处理后当前编辑器聚焦并定位到对应文本块，且记录当前聚焦项序号

- [x] 执行内容：新增底部左右跳转按钮，支持按待处理顺序切换
  - 验收标准：点击左/右按钮可在待处理项之间连续聚焦跳转（含跨页面条目）

- [x] 执行内容：补齐聚焦态可视反馈与回归验证
  - 验收标准：当前聚焦待处理项有明显样式；`npm run test` 与三端 `tsc --noEmit` 通过

状态：`[x] 已完成`

---

## M0.13-R17：子任务链接块前置输入光标修复

### 任务清单

- [x] 执行内容：缩小子任务链接跳转命中范围
  - 验收标准：仅点击链接标题触发跳转，点击链接块非标题区域可用于编辑落点定位

- [x] 执行内容：新增子任务链接时自动补尾随空格
  - 验收标准：连续添加子任务后，链接块之间默认存在可编辑文本落点

- [x] 执行内容：双编辑器一致性修复与回归验证
  - 验收标准：`TaskDetail` 与 `StickyView` 同步生效，`npm run test` 与 `npx tsc -p tsconfig.renderer.json --noEmit` 通过

状态：`[x] 已完成`

---

## M0.13-R17-hotfix：子任务前置光标键盘与点击落点补丁

### 任务清单

- [x] 执行内容：补齐 `taskLink` 左右方向键光标移动逻辑
  - 验收标准：光标位于 `taskLink` 前后时，`ArrowLeft/ArrowRight` 可稳定穿越节点并落在预期位置

- [x] 执行内容：修复编辑容器点击抢焦点导致的落点覆盖
  - 验收标准：点击编辑内容区域时不再被外层 `focus()` 覆盖，仅点击容器空白处时触发聚焦

- [x] 执行内容：双编辑器一致性修复与回归验证
  - 验收标准：`TaskDetail` 与 `StickyView` 同步生效，`npm run test` 与 `npx tsc -p tsconfig.renderer.json --noEmit` 通过

状态：`[x] 已完成`

---

## M3：Markdown 粘贴解析能力补齐

### 任务清单

- [ ] 执行内容：在编辑器 `handlePaste` 中识别 Markdown 文本
  - 验收标准：纯文本粘贴支持段落/标题/列表/任务列表的基础转换

- [ ] 执行内容：保持图片粘贴能力兼容
  - 验收标准：图片粘贴行为不回退，文本粘贴解析不影响图片粘贴

- [ ] 执行内容：新增关键用例测试
  - 验收标准：至少覆盖 3 个 Markdown 粘贴场景

状态：`[ ] 未开始`

---

## M4：P1 增强

### 任务清单

- [ ] 执行内容：提醒 Snooze（5/10/30 分钟）
  - 验收标准：弹窗可一键延后，延后后按新时间再次触发

- [ ] 执行内容：并发编辑冲突提示
  - 验收标准：同任务多窗口冲突时给出明确提示，不影响保存

- [ ] 执行内容：搜索高亮摘要
  - 验收标准：搜索结果可展示命中片段与高亮关键词

状态：`[ ] 未开始`
